{{!-- strategies.handlebars --}}
<div class="container">
    <h1>Stratagems</h1>
    <ul>
        {{#each stratagem}}
            <li>{{this.name}} - {{this.description}}</li>
            <!-- Add any additional strategy details here -->
        {{/each}}
    </ul>
</div>
