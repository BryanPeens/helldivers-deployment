{{!-- campaign.handlebars --}}
<div class="campaign-container">
    <h1>Campaign</h1>
    <div class="campaign">{{#each campaigns as |c| }}
        <ul>
            <div class="campaign-card">
                <h2 class="planetname">{{c.name}}</h2>
                <h4 class="faction">{{c.faction}}</h4>
                <h4 class="player count"><img title = "Player Count" class ="player-count-image"src="{{playercount}}" alt="Player Count"> {{c.players}}</h4>

                <h6 class="biome">Biome: {{c.biome}}</h6>
                <p class="biome_desc">Details: {{c.biomedescription}}</p>
            </div><!-- Add any additional match details here -->
        </ul>

    <ul>
        {{#each campaigns as |c| }}
            <li>
                <p>Name: {{c.name}}</p>
                <p>Planet Index: {{c.planetIndex}}</p>
                <p>Faction: {{c.faction}}</p>
                <p>Players: {{c.players}}</p>
                <p>Health: {{c.health}}</p>
                <p>Max Health: {{c.maxHealth}}</p>
                <p>Percentage: {{c.percentage}}</p>
                <p>Defense: {{c.defense}}</p>
                <p>Major Order: {{c.majorOrder}}</p>
                <p>Biome: {{c.biome}}</p>
                <p>Biome Description: {{c.biomedescription}}</p>
                <!-- Add any additional campaign details here -->
            </li>
     </ul>

        {{/each}}
    </div>
</div>
<div>
    <div slot="campaign-top-content" class="campaign-top-wrapper">
        <div>
            <span>
                <div>

                </div>
            </span>
        </div>
    </div>
</div>
<script src="./js/campaign.js"></script>